# Database Schema Notes

This folder contains starter migrations for Supabase.

## Migrations
- `migrations/001_init.sql` is a template to bootstrap the schema.
- Add new migrations as `002_*.sql`, `003_*.sql`, etc.

## Conventions
- Keep DDL in migrations.
- Document any RLS policies in the same file or here.
